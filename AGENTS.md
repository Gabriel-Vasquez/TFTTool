# Agent Rules

- Read `INIT.md` and `CURRENT.md` before changing the project.
- Keep changes focused and preserve the architecture and scope in the mandate.
- Never expose or commit Riot credentials.
- Prefer official Riot APIs and metadata; mocks cannot replace final real-data validation.
- Test focused behavior after each meaningful change and update `CURRENT.md` at milestones.
- Do not claim completion without evidence for tests, real-data integration, packaging, and acceptance requirements.
